# Local Streamlit secrets - DO NOT commit to git
# For Confluent Cloud or other managed Kafka, set these values

# Docker/local development (default)
KAFKA_BOOTSTRAP_SERVERS = "kafka:9092"

# Uncomment for Confluent Cloud or managed Kafka:
# KAFKA_BOOTSTRAP_SERVERS = "pkc-xxx.region.provider.confluent.cloud:9092"
# KAFKA_USERNAME = "your-api-key"
# KAFKA_PASSWORD = "your-api-secret"
